<h1 align ="center">Welcome to <strong><%= @user.username %>'s </strong>page!</h1>

<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>

<h4 align="center"><%= @user.username %>'s articles</h4>
<div class="center">
  <!--instance variable in the def show action / users_controller that will allow pagination to users-->
  <%= will_paginate @user_articles %>
</div>  

<%= render 'articles/article', obj: @user_articles %>

<div class="center">
  <!--instance variable in the def show action / users_controller that will allow pagination to users-->
  <%= will_paginate @user_articles %>
</div>

